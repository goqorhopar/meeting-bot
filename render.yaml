services:
  - type: web
    name: meeting-bot
    env: docker
    dockerfilePath: ./Dockerfile
    plan: standard
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: ENVIRONMENT
        value: production
