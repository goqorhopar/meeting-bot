{
  "name": "meeting-bot",
  "version": "1.0.0",
  "description": "Bot for recording meetings and updating Bitrix",
  "main": "join-meeting.js",
  "scripts": {
    "start": "python main.py",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "puppeteer": "^21.0.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}